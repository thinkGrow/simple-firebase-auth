/**
 * 1. create a project in firebase console
 * 2 in the proj : install firebase
 * 3. bring firebase config into your project (from site)
 * 4. by using useAuth, create the auth and export it
 * 5. log in page: create a provider GoogleAuthProvider
 * 6. use sign in with popup and pass auth and provider
 */
